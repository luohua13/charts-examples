apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsf-controler.fullname" . }}-test-connection"
  labels:
{{ include "tsf-controler.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tsf-controler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
