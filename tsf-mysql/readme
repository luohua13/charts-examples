
1) you need a  storageclass,so it can dynamic crate pv,pvc
need to using existing pv,pvc,  need to fix

images is public repo,  need to change to index.alauda.cn

2)  helm  install  ./  -f values-production.yaml   --name mysql-ha



